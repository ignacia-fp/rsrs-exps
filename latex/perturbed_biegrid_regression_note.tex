\documentclass[11pt]{article}
\usepackage[margin=0.75in]{geometry}
\usepackage{amsmath}
\usepackage{array}
\usepackage[table]{xcolor}
\usepackage{graphicx}
\usepackage{hyperref}

\title{Perturbed BIEGrid RSRS Regression Changelog}
\author{}
\date{\today}

\newcommand{\ip}[2]{\left\langle #1, #2 \right\rangle}
\newcommand{\fail}[1]{\cellcolor{orange!30}{#1}}
\newcommand{\ok}[1]{#1}

\begin{document}
\maketitle

\section*{Setup}

All runs below are serial 10k BIEGrid RSRS checks: mesh width \texttt{0.01},
10201 points, fixed rank \texttt{k = 40}, oversampling \texttt{p = 40}, and
\texttt{tot\_num\_samples = 1520}. The RSRS sampling seed, perturbation seed,
and error diagnostic seeds are fixed. Seeding initializes the random number
generator at the start of a run; it does not reuse the same sample vector
repeatedly. Each draw advances the generator, and repeating the run with the
same seed reproduces the same sample sequence.

The adjoint check is
\[
    \ip{Ax}{y} \approx \ip{x}{A^*y}.
\]
It is applied to the RSRS approximation and to the inverse/preconditioner path.
When this check fails, the relative 2-norm estimate can become larger than the
relative Frobenius norm estimate.

\section*{Perturbed Operator}

Let $A_0$ denote the unperturbed real symmetric BIEGrid operator. The perturbed
operators have the form $A = A_0 + \Delta$. The vectors $u$ and $v$ are
deterministic random vectors generated from perturbation seed \texttt{12345} and
normalized to have 2-norm one. The perturbation scale is denoted by $s$. For
real perturbations, $\alpha = s$. For complex perturbations,
$\alpha = s(1+2i)/\sqrt{5}$, so $|\alpha| = s$.

For nonsymmetric perturbations,
\[
    \Delta = \alpha u v^*,\qquad
    \Delta x = \alpha u (v^* x),\qquad
    \Delta^* x = \overline{\alpha}\, v (u^* x).
\]
For real data, $v^* = v^T$ and $\overline{\alpha} = \alpha$. For the real
symmetric variant, $v = u$. For the complex symmetric variant,
\[
    \Delta = \alpha u \overline{u}^*,\qquad
    \Delta x = \alpha u (\overline{u}^* x),\qquad
    \Delta^* x = \overline{\alpha}\,\overline{u}(u^* x).
\]
The exact perturbed operator tests compare these \texttt{matvec} and
\texttt{rmatvec} routes against the explicitly assembled dense matrix and its
transpose/adjoint. These exact operator adjoint checks pass at roundoff level;
the failures below are for the RSRS approximation and inverse.

\section*{Change}

The old results use \texttt{bempp/rsrs} commit \texttt{4a2fcb2} with
$s=3{\times}10^{-3}$. The new results use commit \texttt{12a0c28} with
$s=10^{-2}$. The algorithmic changes described below are changes to
\texttt{bempp/rsrs}.

The change separates the treatment of local LU factors used for near-field
updates from the diagonal LU factors used in the final block diagonal solve.
Let the local pivot block be $X_{RR}$, and let its partial-pivoted LU factorization be
$P X_{RR} = L U$. Partial pivoting is row pivoting, so the algebra for applying
$X_{RR}^{-1}$ on the left is not equivalent to applying the same triangular
factors from the right or in an adjoint path. For near-field LU updates, the
pivoted solve is now evaluated during extraction to form dense rectangular
blocks such as
\[
    G_{RN} = X_{RR}^{-1} X_{RN}, \qquad
    G_{NR} = X_{NR} X_{RR}^{-1},
\]
and the resulting update blocks are stored directly. For diagonal LU factors,
the unfactorized pivot block $X_{RR}$ is stored alongside the LU factorization,
so non-inverse transpose/adjoint application can use $X_{RR}^*$ directly rather
than applying row-pivoted triangular factors in an incompatible orientation.

This does not add a second rectangular near-field block: the precomputed update
block replaces the previously stored rectangular block, and the square LU factor
for that near-field update is no longer stored. The storage increase is in the
diagonal LU factors, which retain both the unfactorized pivot block $X_{RR}$ and
the LU factorization.

\section*{Old Results: \texttt{4a2fcb2}, $s=3{\times}10^{-3}$}

\begin{center}
\scriptsize
\resizebox{\textwidth}{!}{%
\begin{tabular}{>{\raggedright\arraybackslash}p{1.55in}
                >{\raggedright\arraybackslash}p{0.7in}
                >{\raggedleft\arraybackslash}p{0.75in}
                >{\raggedleft\arraybackslash}p{0.75in}
                >{\raggedleft\arraybackslash}p{0.75in}
                >{\raggedleft\arraybackslash}p{0.75in}
                >{\raggedleft\arraybackslash}p{0.75in}
                >{\raggedleft\arraybackslash}p{0.75in}}
\hline
Case & RSRS symmetry & $\mathrm{cond}_2(A)$ & rel. 2-norm & rel. Fro. & adjoint check & inverse adjoint & solve error \\
\hline
Unperturbed BIEGrid & \texttt{Symmetric} &
$1.518{\times}10^{4}$ & $9.327{\times}10^{-8}$ & $1.728{\times}10^{-7}$ &
$1.637{\times}10^{-14}$ & $4.415{\times}10^{-14}$ & $1.255{\times}10^{-5}$ \\
Real symmetric perturbed & \texttt{Symmetric} &
$1.518{\times}10^{4}$ & \fail{$7.255{\times}10^{-6}$} & $3.368{\times}10^{-7}$ &
\fail{$1.715{\times}10^{0}$} & \fail{$1.222{\times}10^{0}$} & $2.192{\times}10^{-5}$ \\
Real nonsymmetric perturbed & \texttt{NoSymm} &
$2.467{\times}10^{6}$ & $2.211{\times}10^{-7}$ & $6.615{\times}10^{-7}$ &
$7.827{\times}10^{-15}$ & $2.350{\times}10^{-13}$ & $9.883{\times}10^{-5}$ \\
Complex symmetric perturbed & \texttt{Symmetric} &
$5.208{\times}10^{5}$ & \fail{$1.194{\times}10^{-5}$} & $6.851{\times}10^{-7}$ &
\fail{$4.364{\times}10^{-1}$} & \fail{$1.225{\times}10^{0}$} & $1.391{\times}10^{-4}$ \\
Complex nonsymmetric perturbed & \texttt{NoSymm} &
$9.261{\times}10^{5}$ & \fail{$8.083{\times}10^{20}$} & \fail{$1.355{\times}10^{14}$} &
\fail{$1.000{\times}10^{0}$} & \fail{$1.000{\times}10^{0}$} & \fail{$2.283{\times}10^{17}$} \\
\hline
\end{tabular}
}
\end{center}

\section*{New Results: \texttt{12a0c28}, $s=10^{-2}$}

\begin{center}
\scriptsize
\resizebox{\textwidth}{!}{%
\begin{tabular}{>{\raggedright\arraybackslash}p{1.55in}
                >{\raggedright\arraybackslash}p{0.7in}
                >{\raggedleft\arraybackslash}p{0.75in}
                >{\raggedleft\arraybackslash}p{0.75in}
                >{\raggedleft\arraybackslash}p{0.75in}
                >{\raggedleft\arraybackslash}p{0.75in}
                >{\raggedleft\arraybackslash}p{0.75in}
                >{\raggedleft\arraybackslash}p{0.75in}}
\hline
Case & RSRS symmetry & $\mathrm{cond}_2(A)$ & rel. 2-norm & rel. Fro. & adjoint check & inverse adjoint & solve error \\
\hline
Unperturbed BIEGrid & \texttt{Symmetric} &
$1.518{\times}10^{4}$ & $7.022{\times}10^{-8}$ & $1.746{\times}10^{-7}$ &
$4.551{\times}10^{-14}$ & $3.164{\times}10^{-14}$ & $1.420{\times}10^{-5}$ \\
Real symmetric perturbed & \texttt{Symmetric} &
$1.518{\times}10^{4}$ & $1.320{\times}10^{-7}$ & $4.412{\times}10^{-7}$ &
$2.242{\times}10^{-13}$ & $5.553{\times}10^{-14}$ & $2.888{\times}10^{-5}$ \\
Real nonsymmetric perturbed & \texttt{NoSymm} &
$5.184{\times}10^{7}$ & $1.823{\times}10^{-7}$ & $6.672{\times}10^{-7}$ &
$7.689{\times}10^{-14}$ & $6.540{\times}10^{-13}$ & $1.720{\times}10^{-3}$ \\
Complex symmetric perturbed & \texttt{Symmetric} &
$6.165{\times}10^{5}$ & \fail{$1.829{\times}10^{-5}$} & $6.587{\times}10^{-7}$ &
$1.639{\times}10^{-14}$ & $6.911{\times}10^{-14}$ & $9.847{\times}10^{-5}$ \\
Complex nonsymmetric perturbed & \texttt{NoSymm} &
$1.104{\times}10^{6}$ & \fail{$2.145{\times}10^{16}$} & \fail{$1.481{\times}10^{16}$} &
$5.618{\times}10^{-14}$ & \fail{$1.826{\times}10^{-1}$} & \fail{$1.789{\times}10^{18}$} \\
\hline
\end{tabular}
}
\end{center}

\noindent Orange cells indicate the metrics that fail the regression thresholds,
show the remaining large-error behavior, or violate the consistency check
$\text{rel. 2-norm} \le \text{rel. Fro.}$.

\section*{Current Baseline: \texttt{07d79e1} (Constant Mode)}

The following table records the current clean 10k serial baseline with
\texttt{bempp/rsrs} commit \texttt{07d79e13fd65b28702a74052edbf0bdc29f19575}
and \texttt{fixed\_rank\_sampling\_mode = Constant}. For consistency with the
earlier tables, the apply columns below report relative 2-norm and relative
Frobenius errors,
\[
\frac{\|A - A_{\mathrm{rsrs}}\|_2}{\|A\|_2},\quad
\frac{\|A - A_{\mathrm{rsrs}}\|_F}{\|A\|_F},
\]
the solve columns report
\[
\texttt{err\_solve\_2} = \|I - A_{\mathrm{rsrs}}^{-1}A\|_2,\quad
\texttt{err\_solve\_fro} = \|I - A_{\mathrm{rsrs}}^{-1}A\|_F,
\]
and the final column \texttt{solve\_error\_rhs} is the sampled solve error from
solving with a random right hand side.

\begin{center}
\scriptsize
\resizebox{\textwidth}{!}{%
\begin{tabular}{>{\raggedright\arraybackslash}p{1.55in}
                >{\raggedleft\arraybackslash}p{0.9in}
                >{\raggedleft\arraybackslash}p{0.9in}
                >{\raggedleft\arraybackslash}p{0.8in}
                >{\raggedleft\arraybackslash}p{0.8in}
                >{\raggedleft\arraybackslash}p{0.75in}}
\hline
Case & rel. 2-norm & rel. Fro. & \texttt{err\_solve\_2} & \texttt{err\_solve\_fro} & \texttt{solve\_error\_rhs} \\
\hline
Unperturbed BIEGrid &
$5.657{\times}10^{-8}$ &
$1.579{\times}10^{-7}$ &
$4.171{\times}10^{-4}$ &
$1.281{\times}10^{-3}$ &
$1.521{\times}10^{-5}$ \\
Real symmetric perturbed &
$1.133{\times}10^{-7}$ &
$3.919{\times}10^{-7}$ &
$7.913{\times}10^{-4}$ &
$3.259{\times}10^{-3}$ &
$3.003{\times}10^{-5}$ \\
Real nonsymmetric perturbed &
$1.424{\times}10^{-7}$ &
$5.971{\times}10^{-7}$ &
$8.667{\times}10^{-1}$ &
$1.170{\times}10^{0}$ &
$5.285{\times}10^{-3}$ \\
Complex symmetric perturbed &
$2.064{\times}10^{-7}$ &
$6.587{\times}10^{-7}$ &
$1.181{\times}10^{-2}$ &
$1.202{\times}10^{-2}$ &
$9.847{\times}10^{-5}$ \\
Complex nonsymmetric perturbed &
$1.613{\times}10^{-7}$ &
$6.558{\times}10^{-7}$ &
$1.866{\times}10^{-2}$ &
$1.970{\times}10^{-2}$ &
$1.806{\times}10^{-4}$ \\
\hline
\end{tabular}
}
\end{center}

\end{document}
